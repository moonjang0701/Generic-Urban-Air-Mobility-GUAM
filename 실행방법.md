# 안전 봉투 시뮬레이션 실행 방법

## 🚀 빠른 실행 (추천)

### 1단계: MATLAB 열기
```matlab
% MATLAB 실행 후 작업 디렉토리로 이동
cd /home/user/webapp
```

### 2단계: 상세 보고서 생성 (가장 추천!)
```matlab
run('Exec_Scripts/exam_Paper_DETAILED_Report.m')
```

**결과물**:
- `Safety_Envelope_Report/Detailed_Report.txt` - 모든 계산 과정
- `Safety_Envelope_Report/Detailed_Analysis_Data.xlsx` - 엑셀 데이터 + 공식
- `Safety_Envelope_Report/Analysis_Workspace.mat` - MATLAB 변수들

---

## 📋 다른 실행 옵션들

### 옵션 1: 기본 구현 (시각화 포함)
```matlab
run('Exec_Scripts/exam_Paper_Safety_Envelope_Implementation.m')
```
**출력**: 3D 안전 봉투 그래프, 충돌 확률 맵

### 옵션 2: 올바른 5단계 흐름
```matlab
run('Exec_Scripts/exam_Paper_CORRECT_Flow.m')
```
**출력**: 
1. 성능 측정
2. 봉투 계산
3. 경로 계획
4. 충돌 확률 계산
5. 안전성 검증

### 옵션 3: 현실적인 동적 시뮬레이션
```matlab
run('Exec_Scripts/exam_Paper_Safety_Envelope_REALISTIC.m')
```
**출력**: 90초 비행, 회전 포함, 시간에 따른 봉투 변화

---

## 🔧 실행 전 확인사항

### 1. GUAM 환경 설정 확인
```matlab
% 현재 디렉토리 확인
pwd  % /home/user/webapp 이어야 함

% GUAM 파일 확인
ls Aircraft_Simulations/GUAM/GUAM.slx
```

### 2. 필요한 라이브러리 확인
```matlab
% STARS 라이브러리 경로 확인
which QrotZ
% 결과: /home/user/webapp/lib/... 경로가 나와야 함
```

### 3. Simulink 모델 확인
```matlab
% GUAM 모델 로드 테스트
open('Aircraft_Simulations/GUAM/GUAM.slx')
```

---

## 📊 단계별 상세 실행 방법

### 방법 1: 명령창에서 직접 실행

```matlab
% 1. MATLAB 시작
% 2. 작업 디렉토리로 이동
cd /home/user/webapp

% 3. 스크립트 실행
run('Exec_Scripts/exam_Paper_DETAILED_Report.m')

% 4. 결과 확인
% Safety_Envelope_Report 폴더가 생성됨
ls Safety_Envelope_Report/
```

### 방법 2: MATLAB 편집기에서 실행

```matlab
% 1. 파일 열기
edit Exec_Scripts/exam_Paper_DETAILED_Report.m

% 2. 편집기에서 "Run" 버튼 클릭 (F5)

% 3. 또는 섹션별 실행
% - 커서를 원하는 섹션에 두고
% - Ctrl+Enter (섹션 실행)
```

### 방법 3: 함수 형태로 실행

```matlab
% 작업 디렉토리로 이동
cd /home/user/webapp

% 경로 추가
addpath('Exec_Scripts')

% 실행 (만약 함수로 만들었다면)
exam_Paper_DETAILED_Report
```

---

## 🎯 각 스크립트별 실행 시간

| 스크립트 | 예상 시간 | 비고 |
|---------|----------|------|
| exam_Paper_DETAILED_Report.m | 5-10분 | 4개 테스트 + 보고서 생성 |
| exam_Paper_CORRECT_Flow.m | 8-15분 | 5단계 전체 프로세스 |
| exam_Paper_Safety_Envelope_REALISTIC.m | 3-5분 | 90초 시뮬레이션 |
| exam_Paper_Safety_Envelope_Implementation.m | 2-3분 | 기본 구현 |

---

## 📁 출력 파일 위치

### 상세 보고서 실행 후
```
Safety_Envelope_Report/
├── Detailed_Report.txt          ← 모든 계산 과정
├── Detailed_Analysis_Data.xlsx  ← 엑셀 (데이터 + 공식)
└── Analysis_Workspace.mat       ← MATLAB 변수들
```

### 보고서 내용 확인
```matlab
% 텍스트 파일 읽기
type Safety_Envelope_Report/Detailed_Report.txt

% 엑셀 파일 열기
winopen('Safety_Envelope_Report/Detailed_Analysis_Data.xlsx')

% 또는 MATLAB에서 읽기
data = readtable('Safety_Envelope_Report/Detailed_Analysis_Data.xlsx', 'Sheet', 'Performance_Data');
disp(data)

% MAT 파일 로드
load('Safety_Envelope_Report/Analysis_Workspace.mat')
whos  % 모든 변수 확인
```

---

## ⚠️ 문제 해결

### 문제 1: "simSetup를 찾을 수 없습니다"
```matlab
% 해결: 작업 디렉토리 확인
pwd
% 결과가 /home/user/webapp 가 아니면:
cd /home/user/webapp
```

### 문제 2: "QrotZ를 찾을 수 없습니다"
```matlab
% 해결: STARS 라이브러리 경로 추가
addpath(genpath('lib'))

% 확인
which QrotZ
```

### 문제 3: "GUAM.slx를 열 수 없습니다"
```matlab
% 해결: 모델 경로 확인
ls Aircraft_Simulations/GUAM/

% 또는 절대 경로로 시도
open('/home/user/webapp/Aircraft_Simulations/GUAM/GUAM.slx')
```

### 문제 4: 시뮬레이션이 오류로 중단
```matlab
% 해결 1: 워크스페이스 초기화
clear all
close all
clc

% 해결 2: Simulink 캐시 제거
Simulink.fileGenControl('reset')

% 해결 3: 다시 실행
run('Exec_Scripts/exam_Paper_DETAILED_Report.m')
```

### 문제 5: "Out of memory" 오류
```matlab
% 해결: 메모리 정리
clear all
close all

% Java 메모리 확인
java.lang.Runtime.getRuntime.maxMemory / 1024^3  % GB 단위

% 더 작은 테스트로 시도
run('Exec_Scripts/exam_Paper_Safety_Envelope_Implementation.m')
```

---

## 🔍 실행 결과 확인 방법

### 1. 콘솔 출력 확인
실행 중에 다음과 같은 출력이 나타남:
```
====================================================================
           Safety Envelope Detailed Analysis Report
              Paper Implementation with Full Documentation
====================================================================

SECTION 1: AIRCRAFT PERFORMANCE MEASUREMENT
====================================================================

Step 1.1: Test Overview
----------------------
Total number of tests: 4
Test cruise speeds: 60, 80, 100, 120 knots
Purpose: Measure maximum velocities in all directions
...
```

### 2. 그래프 확인
여러 Figure 창이 열림:
- Figure 1: 3D 안전 봉투 시각화
- Figure 2: 충돌 확률 맵
- Figure 3: 시간에 따른 위치 변화
- Figure 4: 봉투 크기 변화

### 3. 파일 확인
```matlab
% 생성된 파일 목록 확인
ls -lh Safety_Envelope_Report/

% 파일 크기 확인
dir Safety_Envelope_Report/
```

---

## 📝 실행 예제 (완전한 세션)

```matlab
% ===== MATLAB 실행 후 =====

% 1. 작업 디렉토리로 이동
cd /home/user/webapp
pwd  % 확인: /home/user/webapp

% 2. 환경 초기화
clear all
close all
clc

% 3. 경로 확인
addpath(genpath('lib'))
which QrotZ  % STARS 라이브러리 확인

% 4. 상세 보고서 생성 (추천!)
fprintf('상세 보고서 생성 시작...\n');
tic
run('Exec_Scripts/exam_Paper_DETAILED_Report.m')
toc  % 실행 시간 출력

% 5. 결과 확인
fprintf('\n생성된 파일:\n');
ls Safety_Envelope_Report/

% 6. 보고서 읽기
fprintf('\n보고서 내용 (처음 50줄):\n');
type Safety_Envelope_Report/Detailed_Report.txt | head -50

% 7. 엑셀 데이터 확인
data = readtable('Safety_Envelope_Report/Detailed_Analysis_Data.xlsx', ...
                 'Sheet', 'Performance_Data');
disp(data)

% 8. 주요 결과 변수 확인
load('Safety_Envelope_Report/Analysis_Workspace.mat')
fprintf('\n안전 봉투 반경: %.2f m\n', r_eq);
fprintf('안전 봉투 부피: %.0f m³\n', V_envelope);
fprintf('최대 전진 속도: %.2f m/s (%.1f knots)\n', V_f, V_f/0.514444);

% 9. 완료!
fprintf('\n✅ 실행 완료!\n');
```

---

## 🎓 학술 논문용 실행

### 논문에 사용할 결과 추출
```matlab
% 1. 보고서 생성
run('Exec_Scripts/exam_Paper_DETAILED_Report.m')

% 2. 결과 로드
load('Safety_Envelope_Report/Analysis_Workspace.mat')

% 3. 주요 결과 정리
fprintf('=== 논문용 결과 ===\n\n');

fprintf('성능 파라미터:\n');
fprintf('  최대 전진 속도: %.2f m/s (%.1f knots)\n', V_f, V_f/0.514444);
fprintf('  최대 후진 속도: %.2f m/s (%.1f knots)\n', V_b, V_b/0.514444);
fprintf('  최대 상승 속도: %.2f m/s (%.1f ft/s)\n', V_a, V_a/0.3048);
fprintf('  최대 하강 속도: %.2f m/s (%.1f ft/s)\n', V_d, V_d/0.3048);

fprintf('\n안전 봉투:\n');
fprintf('  반축 a (전진): %.2f m\n', a);
fprintf('  반축 b (후진): %.2f m\n', b);
fprintf('  반축 c (상승): %.2f m\n', c);
fprintf('  반축 d (하강): %.2f m\n', d);
fprintf('  반축 e,f (좌우): %.2f m\n', e);

fprintf('\n봉투 크기:\n');
fprintf('  부피: %.0f m³\n', V_envelope);
fprintf('  등가 구 반경: %.2f m\n', r_eq);
fprintf('  최소 안전 거리: %.2f m\n', 2*r_eq);

% 4. 그래프를 이미지로 저장 (논문용)
saveas(gcf, 'Safety_Envelope_Report/Figure_for_Paper.png')
saveas(gcf, 'Safety_Envelope_Report/Figure_for_Paper.eps')  % 고해상도
```

---

## 🚦 단계별 체크리스트

실행 전 체크:
- [ ] MATLAB 실행됨
- [ ] 작업 디렉토리가 `/home/user/webapp`
- [ ] GUAM 모델 파일 존재 확인
- [ ] STARS 라이브러리 경로 확인

실행 중 체크:
- [ ] 콘솔에 진행 상황 출력됨
- [ ] 오류 메시지 없음
- [ ] 시뮬레이션 진행 중 (시간 소요)

실행 후 체크:
- [ ] `Safety_Envelope_Report/` 폴더 생성됨
- [ ] `.txt` 파일 생성됨
- [ ] `.xlsx` 파일 생성됨
- [ ] `.mat` 파일 생성됨
- [ ] 그래프가 표시됨

---

## 💡 팁과 트릭

### 빠른 테스트
```matlab
% 전체 실행 대신 빠른 테스트
run('Exec_Scripts/exam_Paper_Safety_Envelope_Implementation.m')
% 이게 작동하면 상세 보고서도 작동함
```

### 배치 실행
```matlab
% 여러 스크립트를 순차적으로 실행
scripts = {
    'exam_Paper_Safety_Envelope_Implementation.m'
    'exam_Paper_CORRECT_Flow.m'
    'exam_Paper_Safety_Envelope_REALISTIC.m'
    'exam_Paper_DETAILED_Report.m'
};

for i = 1:length(scripts)
    fprintf('\n실행 중: %s\n', scripts{i});
    run(['Exec_Scripts/' scripts{i}]);
    fprintf('완료: %s\n', scripts{i});
end
```

### 결과 비교
```matlab
% 다른 속도로 테스트하고 결과 비교
speeds = [60, 80, 100, 120];  % knots
results = struct();

for i = 1:length(speeds)
    % 속도 설정
    cruise_speed = speeds(i);
    
    % 실행
    run('Exec_Scripts/exam_Paper_Safety_Envelope_Implementation.m')
    
    % 결과 저장
    results(i).speed = cruise_speed;
    results(i).envelope_radius = r_eq;
    results(i).volume = V_envelope;
end

% 비교 출력
for i = 1:length(results)
    fprintf('속도 %d knots: 반경 %.2f m, 부피 %.0f m³\n', ...
            results(i).speed, results(i).envelope_radius, results(i).volume);
end
```

---

## 📞 도움말

### 추가 문서
- `DETAILED_REPORT_GUIDE_KR.md` - 상세 보고서 가이드
- `CORRECT_FLOW_KR.md` - 올바른 흐름 설명
- `ERROR_FIX_KR.md` - 오류 해결 방법
- `PROJECT_COMPLETION_SUMMARY.md` - 전체 프로젝트 요약

### 빠른 명령어
```matlab
% 도움말 보기
help exam_Paper_DETAILED_Report

% 파일 내용 확인
type Exec_Scripts/exam_Paper_DETAILED_Report.m

% 실행 중 중단
Ctrl+C

% 모든 창 닫기
close all

% 워크스페이스 정리
clear all
```

---

**🎯 가장 추천하는 실행 방법**:
```matlab
cd /home/user/webapp
run('Exec_Scripts/exam_Paper_DETAILED_Report.m')
```

이것만 실행하면 모든 결과(텍스트, 엑셀, 그래프)가 생성됩니다! ✅
